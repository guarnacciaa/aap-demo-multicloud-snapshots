---
controller_execution_environments:
  - name: "{{ demo_execution_environment_name }}"
    organization: "{{ demo_organization_name }}"
    image: quay.io/ansible/ansible-runner:latest
    description: >-
      Custom EE for multicloud snapshots. After building and pushing to Private
      Automation Hub, set image to <aap_host>/ee-multicloud-snapshots:latest
      (see docs/setup.md).
