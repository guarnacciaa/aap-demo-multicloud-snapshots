# Description: Provision a demo Azure VM with all required networking resources.
# This playbook creates a complete Azure environment from scratch: resource group,
# virtual network, subnet, NSG, public IP, NIC, and the VM itself.
# Run before snapshot playbooks. Safe to re-run (idempotent).
# Counterpart: deprovision_azure_vm.yml (destroys all resources created here).
# Authenticates via the Service Principal credentials AAP injects (default) or,
# when azure_auth_mode: msi, via Managed Identity — see the auth_source
# parameter on each azure_rm_* task below and docs/setup.md.
---
- name: Provision Azure demo VM and networking for multicloud snapshots
  hosts: localhost
  gather_facts: true
  collections:
    - azure.azcollection
    - ansible.builtin

  vars:
    demo_tag: multicloud-snapshots

  pre_tasks:
    # Play vars must not reference their own name (recursive loop). Resolve defaults here instead.
    - name: Validate required Azure provisioning variables
      ansible.builtin.assert:
        that:
          - azure_vm_hostname is defined
          - azure_resource_group is defined
          - azure_vm_admin_password is defined
        fail_msg: >-
          Set azure_vm_hostname, azure_resource_group, and azure_vm_admin_password
          in demo_variables.yml or job template extra variables before provisioning.
      tags:
        - always

    - name: Resolve Azure provisioning variables with defaults
      ansible.builtin.set_fact:
        target_hostname: "{{ azure_vm_hostname }}"
        azure_location: "{{ azure_location | default('eastus') }}"
        azure_vm_size: "{{ azure_vm_size | default('Standard_B2s') }}"
        azure_vm_admin_user: "{{ azure_vm_admin_user | default('azureuser') }}"
        azure_vnet_name: "{{ azure_vnet_name | default('demo-multicloud-vnet') }}"
        azure_vnet_cidr: "{{ azure_vnet_cidr | default('10.0.0.0/16') }}"
        azure_subnet_name: "{{ azure_subnet_name | default('demo-multicloud-subnet') }}"
        azure_subnet_cidr: "{{ azure_subnet_cidr | default('10.0.1.0/24') }}"
        azure_nsg_name: "{{ azure_nsg_name | default('demo-multicloud-nsg') }}"
        azure_public_ip_name: "{{ azure_public_ip_name | default(azure_vm_hostname + '-pip') }}"
        azure_nic_name: "{{ azure_nic_name | default(azure_vm_hostname + '-nic') }}"
        azure_vm_image: "{{ azure_vm_image | default({'publisher': 'RedHat', 'offer': 'RHEL', 'sku': '9_4', 'version': 'latest'}) }}"
      tags:
        - always

  tasks:
    # AAP's Azure RM credential type has no Managed Identity input, so it can
    # only inject Service Principal env vars (AZURE_CLIENT_ID/SECRET/TENANT).
    # To support Managed Identity too, every azure_rm_* task below passes
    # auth_source explicitly: "msi" when azure_auth_mode: msi (the host running
    # this job must then be an Azure resource with MSI enabled — see
    # docs/setup.md), otherwise omitted so the module falls back to its
    # default "auto" source (module params -> env -> credential_file -> cli),
    # which picks up the Service Principal env vars AAP already injected.
    #
    # Orphaned OS disks block reprovisioning: if the VM does not exist but its
    # disk was left behind (e.g. from a partial deletion), Azure raises
    # ConflictingUserInput and refuses CreateOption.FromImage. Remove the disk
    # unconditionally before creating the VM so the playbook stays idempotent.
    - name: Remove any orphaned OS disk with the same name as the target VM
      azure.azcollection.azure_rm_manageddisk:
        resource_group: "{{ azure_resource_group }}"
        name: "{{ target_hostname }}"
        state: absent
        auth_source: "{{ 'msi' if (azure_auth_mode | default('service_principal') == 'msi') else omit }}"
      ignore_errors: true
      tags:
        - azure
        - provision
        - vm

    - name: Ensure Azure resource group exists
      azure.azcollection.azure_rm_resourcegroup:
        name: "{{ azure_resource_group }}"
        location: "{{ azure_location }}"
        tags:
          Demo: "{{ demo_tag }}"
        auth_source: "{{ 'msi' if (azure_auth_mode | default('service_principal') == 'msi') else omit }}"
      tags:
        - azure
        - provision
        - network

    - name: Create virtual network for the demo environment
      azure.azcollection.azure_rm_virtualnetwork:
        resource_group: "{{ azure_resource_group }}"
        name: "{{ azure_vnet_name }}"
        address_prefixes_cidr:
          - "{{ azure_vnet_cidr }}"
        tags:
          Demo: "{{ demo_tag }}"
        auth_source: "{{ 'msi' if (azure_auth_mode | default('service_principal') == 'msi') else omit }}"
      tags:
        - azure
        - provision
        - network

    - name: Create subnet within the demo virtual network
      azure.azcollection.azure_rm_subnet:
        resource_group: "{{ azure_resource_group }}"
        virtual_network_name: "{{ azure_vnet_name }}"
        name: "{{ azure_subnet_name }}"
        address_prefix_cidr: "{{ azure_subnet_cidr }}"
        auth_source: "{{ 'msi' if (azure_auth_mode | default('service_principal') == 'msi') else omit }}"
      tags:
        - azure
        - provision
        - network

    # Allow SSH for management; tighten rules for production use.
    - name: Create network security group with SSH access
      azure.azcollection.azure_rm_securitygroup:
        resource_group: "{{ azure_resource_group }}"
        name: "{{ azure_nsg_name }}"
        rules:
          - name: AllowSSH
            protocol: Tcp
            destination_port_range: 22
            access: Allow
            priority: 1000
            direction: Inbound
        tags:
          Demo: "{{ demo_tag }}"
        auth_source: "{{ 'msi' if (azure_auth_mode | default('service_principal') == 'msi') else omit }}"
      tags:
        - azure
        - provision
        - network

    - name: Create public IP address for the demo VM
      azure.azcollection.azure_rm_publicipaddress:
        resource_group: "{{ azure_resource_group }}"
        name: "{{ azure_public_ip_name }}"
        allocation_method: Static
        sku: Standard
        tags:
          Demo: "{{ demo_tag }}"
        auth_source: "{{ 'msi' if (azure_auth_mode | default('service_principal') == 'msi') else omit }}"
      register: azure_pip_result
      tags:
        - azure
        - provision
        - network

    - name: Create network interface and attach to subnet and NSG
      azure.azcollection.azure_rm_networkinterface:
        resource_group: "{{ azure_resource_group }}"
        name: "{{ azure_nic_name }}"
        virtual_network: "{{ azure_vnet_name }}"
        subnet_name: "{{ azure_subnet_name }}"
        security_group: "{{ azure_nsg_name }}"
        ip_configurations:
          - name: default
            public_ip_address_name: "{{ azure_public_ip_name }}"
            primary: true
        tags:
          Demo: "{{ demo_tag }}"
        auth_source: "{{ 'msi' if (azure_auth_mode | default('service_principal') == 'msi') else omit }}"
      tags:
        - azure
        - provision
        - network

    - name: Provision the Azure demo VM with managed OS disk
      azure.azcollection.azure_rm_virtualmachine:
        resource_group: "{{ azure_resource_group }}"
        name: "{{ target_hostname }}"
        vm_size: "{{ azure_vm_size }}"
        admin_username: "{{ azure_vm_admin_user }}"
        admin_password: "{{ azure_vm_admin_password }}"
        network_interfaces:
          - "{{ azure_nic_name }}"
        image:
          publisher: "{{ azure_vm_image.publisher }}"
          offer: "{{ azure_vm_image.offer }}"
          sku: "{{ azure_vm_image.sku }}"
          version: "{{ azure_vm_image.version }}"
        os_disk_size_gb: 64
        managed_disk_type: Standard_LRS
        tags:
          Demo: "{{ demo_tag }}"
          Hostname: "{{ target_hostname }}"
        auth_source: "{{ 'msi' if (azure_auth_mode | default('service_principal') == 'msi') else omit }}"
      register: azure_vm_result
      tags:
        - azure
        - provision
        - vm

    - name: Publish provisioned VM details for downstream workflow nodes
      ansible.builtin.set_stats:
        data:
          azure_vm_id: "{{ azure_vm_result.ansible_facts.azure_vm.id }}"
          azure_vm_public_ip: "{{ azure_pip_result.state.ip_address }}"
          azure_vm_provisioned: true
      tags:
        - azure
        - provision

    - name: Display Azure VM provisioning summary
      ansible.builtin.debug:
        msg: >-
          Azure VM '{{ target_hostname }}' provisioned in resource group
          '{{ azure_resource_group }}' ({{ azure_location }}).
          Public IP: {{ azure_pip_result.state.ip_address }}.
      tags:
        - azure
        - provision
