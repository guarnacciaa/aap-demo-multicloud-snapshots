---
# Certified collections for cloud provisioning and snapshot playbooks.
# Do not set per-collection source URLs here; ansible-galaxy resolves servers
# from ../ansible.cfg (automation_hub_certified / automation_hub_validated).
# CasC (infra.aap_configuration) is installed locally, not in this EE.
collections:
  - name: azure.azcollection
  - name: amazon.aws
