---
# Build a custom EE when the platform default lacks Azure/AWS SDK dependencies.
# See docs/setup.md for ansible-builder instructions.
#
# ee-minimal-rhel9 does not ship pip. python3-pip is installed in prepend_builder
# (the builder stage) so the assemble script can install azure.azcollection and
# amazon.aws Python dependencies. This does not affect the galaxy introspect step
# (a separate stage) and does not add pip to the final image.
version: 3

build_arg_defaults:
  ANSIBLE_GALAXY_CLI_COLLECTION_OPTS: "-v"

images:
  base_image:
    name: registry.redhat.io/ansible-automation-platform-26/ee-minimal-rhel9:latest

# ee-minimal-rhel9 uses microdnf, not dnf (AAP 2.6 EE builder reference).
options:
  package_manager_path: /usr/bin/microdnf

dependencies:
  galaxy: requirements.yml

# Project-root ansible.cfg (Automation Hub token) is required before build.
additional_build_files:
  - src: ../ansible.cfg
    dest: configs

additional_build_steps:
  prepend_galaxy:
    # Use the same Hub server_list and token as local collection installs.
    - COPY _build/configs/ansible.cfg /etc/ansible/ansible.cfg
  prepend_builder:
    # Install pip in the builder stage so the assemble script can install
    # Python requirements declared by azure.azcollection and amazon.aws.
    - RUN microdnf install -y python3-pip && microdnf clean all
  append_final:
    # Do not ship ansible.cfg in the final image (may contain tokens).
    - RUN rm -f /etc/ansible/ansible.cfg
