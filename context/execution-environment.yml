---
# Build a custom EE when the platform default lacks Azure/AWS SDK dependencies.
# See docs/setup.md for ansible-builder instructions.
#
# ee-supported-rhel9 installs Ansible and its dependencies for Python 3.12.
# $PYCMD defaults to /usr/bin/python3 (Python 3.9) in the builder stages, so
# ENV PYCMD=/usr/bin/python3.12 is set in both prepend_builder and prepend_final
# to ensure introspect.py, check_ansible, and assemble all target Python 3.12.
version: 3

build_arg_defaults:
  ANSIBLE_GALAXY_CLI_COLLECTION_OPTS: "-v"

images:
  base_image:
    name: registry.redhat.io/ansible-automation-platform-27/ee-supported-rhel9:latest

# Both ee-minimal-rhel9 and ee-supported-rhel9 use microdnf, not dnf.
# Without this override the assemble script calls /usr/bin/dnf and fails.
options:
  package_manager_path: /usr/bin/microdnf

dependencies:
  galaxy: requirements.yml

# Project-root ansible.cfg (Automation Hub token) is required before build.
additional_build_files:
  - src: ../ansible.cfg
    dest: configs

additional_build_steps:
  prepend_galaxy:
    # Use the same Hub server_list and token as local collection installs.
    - COPY _build/configs/ansible.cfg /etc/ansible/ansible.cfg
  prepend_builder:
    # Override $PYCMD to python3.12: ee-supported-rhel9 installs pyyaml and
    # packaging for Python 3.12, so introspect.py must run with that interpreter.
    - ENV PYCMD=/usr/bin/python3.12
    # C headers for systemd-python and dbus-python (azure.azcollection transitive
    # deps not declared in bindep.txt). python3.12-devel provides Python.h.
    - RUN microdnf install -y systemd-devel dbus-devel glib2-devel python3.12-devel && microdnf clean all
  prepend_final:
    # Same PYCMD override so check_ansible and assemble target Python 3.12,
    # where Ansible and Ansible Runner are actually installed in this image.
    - ENV PYCMD=/usr/bin/python3.12
    # gcc and C headers needed by pip to compile extension packages during the
    # assemble step: systemd-python, dbus-python, and gssapi/krb5.
    - RUN microdnf install -y gcc krb5-devel systemd-devel dbus-devel glib2-devel python3.12-devel && microdnf clean all
  append_final:
    # Do not ship ansible.cfg in the final image (may contain tokens).
    - RUN rm -f /etc/ansible/ansible.cfg
